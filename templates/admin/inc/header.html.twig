<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('admin_index') }}">phpantry.com</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_index') }}"><i class="fas fa-solar-panel"></i> Dashboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_users') }}"><i class="fas fa-user-tie"></i> Users</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_jobs') }}"><i class="fas fa-ad"></i> Jobs</a>
            </li>
        </ul>
        <div class="mr-4 site_view">
            <a href="{{ path('home_page') }}"><i class="far fa-eye"></i> View website</a>
        </div>
        <a class="btn btn-danger" href="{{ path('logout') }}" role="button">Sign Out</a>
    </div>
</nav>
{% for class, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-dismissible alert-{{ class }} container mt-4 text-center">
            <button type="button" class="close" data-dismiss="alert">×</button>
            <h4 class="alert-heading">Success</h4>
            <p class="mb-0">{{ message }}</p>
        </div>
    {% endfor %}
{% endfor %}

